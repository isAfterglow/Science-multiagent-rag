# MOOSE Research Copilot

## 项目定位

这是一个面向 MOOSE 仿真的、证据驱动的多智能体科研协作系统。系统统一利用论文、MOOSE 输入文件、历史仿真 case、运行日志与定量结果，支持科学问答、参数影响分析、受控仿真计划、人工审批、真实执行和结果复盘。

第一版聚焦现有热防护材料温度误差校准流程，不试图构建通用的全自动仿真平台。

## 核心演示闭环

```text
科研问题
  -> 检索文献和历史仿真证据
  -> 分析参数与指标
  -> 多智能体审查证据
  -> 生成受限仿真计划
  -> 人工审批
  -> 运行小批量 MOOSE 仿真
  -> 验证结果并生成带引用的复盘报告
```

## 阶段 1：仿真知识底座

目标：让现有仿真资产可可靠查询、可追溯、可用于科学问答。

- 导入现有 LHS 参数表、MOOSE case 状态、输入文件、运行日志、point-value 曲线、排名表、历史报告和模型产物。
- 定义 `SimulationCase`、`RunArtifact`、`MetricRecord`、`EvidenceCard`、`SimulationPlan` 等数据契约。
- 建立 SQLite 或 DuckDB 仿真 Registry。
- 对论文、MOOSE 文档、输入文件说明、历史报告和日志建立混合 RAG：向量检索 + BM25 + Query Expansion + RRF。
- 实现只读分析工具：case/指标检索、Top-K 对比、参数相关性、曲线摘要和失败日志检索。
- 建立 20-25 题的基础评测集。

交付物：可复现的数据导入与索引脚本、Registry 查询工具、带引用的单 Agent 回答与基线评测报告。

## 阶段 2：证据驱动的多智能体协作

目标：让多智能体的职责、协作过程和增益可以被清楚观测和验证。

```text
Supervisor
  -> Retriever / Evidence Agent
  -> Simulation Analyst Agent
  -> Critic Agent
  -> Synthesizer / Reviewer
```

- 用 LangGraph 实现路由、检索/数据分析并行分支、审核反馈与有限次数回退。
- Agent 间通过经 Pydantic 校验的 JSON 交换信息，不传递无约束的长文本消息。
- 生成带引用的 `EvidenceCard`：结论、适用条件、来源、支持片段或数值、置信度与不确定性。
- Critic Agent 专门识别结论冲突、证据不足、样本量不足和参数混杂。
- 最终回答只能基于 Evidence Card 和程序生成的数值分析结果。
- 将评测集扩展到 35-40 题，对比纯 RAG、RAG + 数据工具、完整多 Agent 三种方案。

核心指标：数值答案准确率、引用支撑率、无证据断言率、冲突发现率、Reviewer 拦截率与 P95 延迟。

## 阶段 3：受控 MOOSE 执行与最终交付

目标：完成从“基于证据提出建议”到“经过审批后验证建议”的闭环。

- 定义受限 `SimulationPlan`：批准的模板版本、允许修改的参数与范围、采样方法、最大 case 数、目标指标、资源预算和停止条件。
- 将现有 LHS 生成和 MOOSE 批量运行脚本封装为经过校验的工具接口；模型不能执行任意 Shell 命令。
- 所有写入或 MOOSE 执行必须经过人工审批；第一版每次仅允许运行 1-5 个 case。
- 记录输入哈希、stdout/stderr、耗时、状态、结果产物和图表；将通过验证的结果写回仿真 Registry。
- 训练轻量代理模型，仅用于候选 case 排序；MOOSE 仍是高保真验证来源。
- 增加紧凑工作台，展示 Agent Trace、证据卡、仿真计划、审批状态、运行日志和复盘报告。
- 内部工具接口稳定后，可选地将只读工具和已批准任务提交能力封装为本地 MOOSE MCP Server。
- 完成 40-50 题评测集、方案对比报告、架构图、可复现实演脚本和 README。

## 范围边界

- 本项目不重复构建多租户 RBAC、复杂长期记忆或通用 SaaS 平台，这些能力已在 Text2SQL Agent 项目中体现。
- 不允许未经审批的大规模仿真、任意输入文件修改或任意 Shell 执行。
- MCP 不是核心闭环的前置条件，必须在内部工具接口稳定后再接入。
