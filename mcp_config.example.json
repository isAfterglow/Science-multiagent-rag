{
  "mcpServers": {
    "moose-research": {
      "command": "conda",
      "args": [
        "run",
        "--no-capture-output",
        "-n",
        "scitime-agent",
        "python",
        "-m",
        "app.mcp_server"
      ]
    }
  }
}
